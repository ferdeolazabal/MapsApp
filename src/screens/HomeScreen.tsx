import { BtnMyLocaction, MapView, ReactLogo, SearchBar } from "../components"

export const HomeScreen = () => {
    return (
        <>
            <MapView />
            <BtnMyLocaction />
            <ReactLogo />
            <SearchBar />
        </>
    )
}
